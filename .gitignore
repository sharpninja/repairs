transcripts/

AGENTS-README-FIRST.yaml
.mcpServer/

# secrets + local env (never commit)
.env
server/.env

# dependencies + generated protobuf code (regenerated by buf in the image)
node_modules/
server/node_modules/
server/gen/

# store packaging build outputs (scripts/package-*.ps1)
dist/

# logs + local state
*.log
mcp.db
cache/
session*.jsonl
todo-bootstrap.marker
index-screenshot.png

# MCP Server storage (off-limits; not app data)
docs/todo.yaml

# local tooling
.claude/
AGENTS-README-FIRST.yaml.deleted-*
